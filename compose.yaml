services:
  letter-quest:
    build: .
    image: letter-quest:latest
    container_name: letter-quest
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data:Z
    environment:
      PORT: "8080"
      DATA_DIR: /data
      # LETTER_QUEST_PIN: "change-me"
    restart: unless-stopped
